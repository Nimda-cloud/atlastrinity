# Vibe CLI: Аналіз Підходів та Рекомендація

**Дата**: 2026-01-25  
**Статус**: 🎯 РЕКОМЕНДАЦІЯ ГОТОВА

---

## 🔍 ПОТОЧНИЙ СТАН

### **У Requirements.txt**
```
mistral-vibe>=1.0.0  # рядок 15
```

### **У Коді (vibe_server.py)**
```python
def resolve_vibe_binary() -> str | None:
    """Resolve the path to the Vibe CLI binary."""
    # Try ~/.local/bin first (common location)
    local_bin = os.path.expanduser("~/.local/bin/vibe")
    if os.path.exists(local_bin):
        return local_bin
    
    # Search PATH
    found = shutil.which(VIBE_BINARY)
```

### **Проблема**
- ✅ Код шукає **CLI binary** через PATH
- ❌ У requirements.txt є **pip пакет** `mistral-vibe`
- 🔴 На системі **НІ binary, НІ pip модуль** не встановлені

**Висновок**: Конфлікт між підходами. Код використовує binary, але requirements.txt має pip пакет.

---

## 📊 ПОРІВНЯННЯ ПІДХОДІВ

### **1. Pip Пакет (`mistral-vibe`)**

#### Переваги:
- ✅ Простий `pip install mistral-vibe`
- ✅ Керування через requirements.txt
- ✅ Версіонування разом з Python залежностями

#### Недоліки:
- ❌ **НЕ офіційний підхід** від Mistral
- ❌ Може бути застарілим (pip пакети оновлюються рідше)
- ❌ Конфлікти з іншими Python залежностями
- ❌ Обмежена функціональність (може не підтримувати всі CLI фічі)
- ❌ Потребує Python environment

### **2. CLI Binary (curl install)**

#### Переваги:
- ✅ **Офіційний підхід** Mistral AI
- ✅ Автоматичні оновлення через `vibe update`
- ✅ Повна функціональність CLI
- ✅ Незалежний від Python environment
- ✅ Швидший запуск (нативний binary)
- ✅ Підтримується безпосередньо Mistral

#### Недоліки:
- ⚠️ Потрібен окремий install step
- ⚠️ Не управляється через pip

### **Офіційна Документація Mistral**

З `@/Users/hawk/Documents/GitHub/atlastrinity/src/brain/services_manager.py:292-303`:
```python
logger.info("[Services] Installing Mistral Vibe CLI via official script...")
result = subprocess.run(
    "curl -LsSf https://mistral.ai/vibe/install.sh | bash",
    shell=True,
    ...
)
```

**Джерело**: https://docs.mistral.ai/vibe/

---

## 🎯 РЕКОМЕНДАЦІЯ

### **ВИКОРИСТОВУВАТИ CLI BINARY**

#### Причини:
1. **Офіційний підхід** від Mistral AI
2. **Код вже реалізований** для binary (vibe_server.py, services_manager.py)
3. **Краща підтримка** та оновлення
4. **Незалежність** від Python залежностей
5. **Повна функціональність** CLI

#### Що треба зробити:
1. ✅ **Видалити** `mistral-vibe>=1.0.0` з requirements.txt
2. ✅ **Залишити** install через curl в setup scripts
3. ✅ **Додати** перевірку binary в dev_setup.py

---

## 🛠️ ПЛАН ЗМІН

### **1. Requirements.txt**
```diff
- mistral-vibe>=1.0.0
+ # Vibe CLI встановлюється окремо через: curl -LsSf https://mistral.ai/vibe/install.sh | bash
```

### **2. setup_dev.py**
Додати перевірку Vibe CLI:
```python
def check_vibe_cli():
    """Перевіряє встановлення Vibe CLI"""
    if shutil.which("vibe"):
        print_success("Vibe CLI знайдено")
        return True
    else:
        print_warning("Vibe CLI не знайдено")
        print_info("Встановіть командою: curl -LsSf https://mistral.ai/vibe/install.sh | bash")
        return False
```

### **3. Документація**
Оновити README/SETUP з інструкцією:
```bash
# Встановлення Vibe CLI (офіційна команда)
curl -LsSf https://mistral.ai/vibe/install.sh | bash
```

---

## ✅ ПІДСУМОК

| Критерій | Pip Package | CLI Binary |
|----------|-------------|------------|
| **Офіційність** | ❌ Неофіційний | ✅ Офіційний |
| **Оновлення** | ⚠️ Через pip | ✅ Автоматичні |
| **Функціональність** | ⚠️ Обмежена | ✅ Повна |
| **Інтеграція з кодом** | ❌ Не використовується | ✅ Вже реалізовано |
| **Незалежність** | ❌ Потребує Python | ✅ Standalone |
| **Швидкість** | ⚠️ Python overhead | ✅ Native binary |

**Вибір: CLI Binary через curl install** 

---

## ІНСТРУКЦІЯ ДЛЯ КОРИСТУВАЧА

### **Встановлення Vibe CLI (one-time)**
```bash
curl -LsSf https://mistral.ai/vibe/install.sh | bash
```

### **Перевірка**
```bash
which vibe
vibe --version
```

### **Оновлення (коли потрібно)**
```bash
vibe update
```

**Готово!** Vibe CLI буде доступний системі через PATH. ✅
