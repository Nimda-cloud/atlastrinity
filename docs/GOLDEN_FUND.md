# 🔱 Golden Fund: The AtlasTrinity Knowledge Base

**Golden Fund** — це централізоване сховище "золотих" знань в екосистемі AtlasTrinity. На відміну від короткострокової пам'яті (Short-term context), Golden Fund зберігає верифіковані дані, документи та факти, які залишаються доступними назавжди.

## 🎯 Призначення

1.  **Персистентність**: Дані зберігаються локально в `~/.config/atlastrinity/data` і переживають перезавантаження, оновлення та навіть перевстановлення системи.
2.  **Автономне Збагачення**: Якщо ви запитуєте про щось, чого немає в базі, Golden Fund може самостійно зайты на портали відкритих даних (наприклад, [data.gov.ua](https://data.gov.ua)), завантажити відповідні датасети, розпарсити їх і дати відповідь.
3.  **Векторний Пошук**: Розуміє _сенс_ запиту, а не тільки ключові слова.

## 🏗 Як це працює?

### 1. Зберігання (Storage)

Система використовує гібридний підхід:

- **ChromaDB**: Векторна база даних для семантичного пошуку (embeddings).
- **SQL Storage**: SQLite для структурованих табличних даних та SQL-запитів.
- **Blob Storage**: Локальне файлове сховище для сирих даних (JSON, CSV, XML, Excel, Parquet), симулює поведінку S3/MinIO.

### 2. Recursive Enrichment (Рекурсивне Збагачення)

Коли ви шукаєте інформацію:

1.  Система спочатку шукає локально у **Vector Store**.
2.  Якщо впевненість (confidence) низька, активується **Enricher**.
3.  Enricher робить запит до зовнішніх джерел (CKAN API).
4.  Знайдені дані автоматично:
    - **Завантажуються** (Ingest)
    - **Трансформуються** до єдиного формату (Unified Schema)
    - **Векторизуються** і зберігаються.
5.  Система повторює пошук вже по нових даних.

## 🛠 Інструменти для Агентів

- `search_golden_fund`: Головний інструмент пошуку.
- `ingest_dataset`: Примусове завантаження даних за URL.
- `store_blob` / `retrieve_blob`: Робота з файлами.

## 📊 Статус та Метрики

- **Tier**: 2 (High Priority)
- **Health Check**: Включено в загальний моніторинг (`python3 scripts/check_mcp_health.py`).
- **Backup**: Включено в `scripts/setup_dev.py` (папка `data/golden_fund` бекапиться автоматично).

---

_Див. також: Технічна документація в `src/mcp_server/golden_fund/README.md`_
